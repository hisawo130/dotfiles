---
name: user_profile
description: User role, platforms, language, and working style preferences
type: user
---

Web design / frontend implementation specialist.

**Primary platforms:**
- Shopify: Dawn theme, Online Store 2.0, Liquid + JSON schema
- ecforce: Liquid templates (`.html.liquid`), admin file uploader for assets, `{{ file_root_path }}` for asset URLs. **Save = immediate production** on active theme — always duplicate first.

**Timezone:** JST

**Language:** Japanese for all communication, English for code comments only

**Working style:**
- Autonomous operation strongly preferred — proceed without asking for confirmation except for destructive/external actions
- Conclusion-first responses with no preamble or filler
- Agent architecture in dotfiles: researcher (Haiku) → planner → executor → reviewer (Sonnet)
- Dotfiles repo at `~/dotfiles/` symlinks: CLAUDE.md, settings.json, agents/, commands/
